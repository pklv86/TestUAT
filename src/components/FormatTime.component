<apex:component access="global" controller="FormatDateTime">
     <apex:attribute assignTo="{!dateTimeValue}" description="The DateTime value to be rendered based upon the user's
               locale" name="date_Timevalue" type="DateTime"></apex:attribute>
       {!TimeZoneValue}
     <apex:attribute assignTo="{!dateValue}" description="The Date value to be rendered based upon the user's
               locale" name="date_value" type="Date"></apex:attribute>
       {!DateFormat}   
</apex:component>